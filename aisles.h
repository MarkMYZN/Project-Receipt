#ifndef AISLES_H
#define AISLES_H

struct beverage {
    char itemName[265];
    int itemCount[256];
    double itemPrice[256];
}

struct cleaning {
    char itemName[265];
    int itemCount[256];
    double itemPrice[256];
}

struct personalCare {
    char itemName[265];
    int itemCount[256];
    double itemPrice[256];
}

struct breakfast {
    char itemName[265];
    int itemCount[256];
    double itemPrice[256];
}

struct frozen {
    char itemName[265];
    int itemCount[256];
    double itemPrice[256];
}

#endif 