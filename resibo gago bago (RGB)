#include <stdio.h>
#include <windows.h>

#define MAX_ITEMS 10

char name[MAX_ITEMS][50]; 
int quantity[MAX_ITEMS];
float price[MAX_ITEMS];

void checkoutCart() {
    printf("Items in the cart:\n");
    printf("%-15s %-10s %-10s\n", "Name", "Quantity", "Price");
    for (int i = 0; i < MAX_ITEMS; ++i) {
        printf("%-15s %-10d %-10.2f\n", name[i], quantity[i], price[i]);
    }
}


void displayMain() {
    printf("Where would you like to go?\n");
    printf("(1) Beverages Aisle\n");
    printf("(2) Cleaning Supplies Aisle\n");
    printf("(3) Personal Care Aisle\n");
    printf("(4) Breakfasts Aisle\n");
    printf("(5) Frozen Aisle\n");
}

void displayFunc() {
    int displayFunction;
    scanf("%d", &displayFunction);

    switch (displayFunction) {
        case 1: // beverage aisle
            system("cls");
            printf("You are in Beverage Aisle.\n");
            printf("Items in shelf:\n");
            beverage();
        case 2: // beverage aisle
            system("cls");
            printf("You are in Cleaning Supplies Aisle.\n");
            printf("Items in shelf:\n");
            cleaning();
        case 3: // beverage aisle
            system("cls");
            printf("You are in Personal Care Aisle.\n");
            printf("Items in shelf:\n");
            personal();
        case 4: // beverage aisle
            system("cls");
            printf("You are in Breakfasts Aisle.\n");
            printf("Items in shelf:\n");
            breakfast();
        case 4: // beverage aisle
            system("cls");
            printf("You are in Frozen Aisle.\n");
            printf("Items in shelf:\n");
            frozen();
    }
}

//Aisles
void beverage() {
    printf("(1) Product 1..........$2\n");
    printf("(2) Product 2..........$3\n");
    printf("(3) Product 3..........$1\n");
    printf("(4) Product 4..........$4\n");
    printf("(5) Product 5..........$2\n");
    beverageFunc();
}
void beverageFunc() {
    int bev;
    scanf("%d", &bev);

    switch (bev) {
        case 1: 
            system("cls");
            for (int i = 0; i < 1; ++i) {
                sprintf(name[i], "tite", i + 1);
                quantity[i] = i + 1;
                price[i] = 2.5 * (i + 1);
            }
            printf("Item Added to Cart.\n");
            printf("\nReady to head out?\n");
            printf("(8) Go back to lobby\n");
            printf("(9) Go to checkout\n");
            printf("Enter your choice: ");
            exitLobby();
            break;
        case 2:
            system("cls");
            for (int i = 1; i < 2; ++i) {
                sprintf(name[i], "pepe", i + 2);
                quantity[i] = i + 2;
                price[i] = 2.5 * (i + 2);
            }
            printf("Item Added to Cart.\n");
            printf("\nReady to head out?\n");
            printf("(8) Go back to lobby\n");
            printf("(9) Go to checkout\n");
            printf("Enter your choice: ");
            exitLobby();
            break;
        case 3:
            system("cls");
            for (int i = 2; i < 3; ++i) {
                sprintf(name[i], "puke", i + 3);
                quantity[i] = i + 3;
                price[i] = 2.5 * (i + 3);
            }
            printf("Item Added to Cart.\n");
            printf("\nReady to head out?\n");
            printf("(8) Go back to lobby\n");
            printf("(9) Go to checkout\n");
            printf("Enter your choice: ");
            exitLobby();
            break;
        case 4:
            system("cls");
            for (int i = 3; i < 4; ++i) {
                sprintf(name[i], "lumpia", i + 4);
                quantity[i] = i + 4;
                price[i] = 2.5 * (i + 4);
            }
            printf("Item Added to Cart.\n");
            printf("\nReady to head out?\n");
            printf("(8) Go back to lobby\n");
            printf("(9) Go to checkout\n");
            printf("Enter your choice: ");
            exitLobby();
            break;
        case 5:
            system("cls");
            for (int i = 4; i < 5; ++i) {
                sprintf(name[i], "siomai", i + 5);
                quantity[i] = i + 5;
                price[i] = 2.5 * (i + 5);
            }
            printf("Item Added to Cart.\n");
            printf("\nReady to head out?\n");
            printf("(8) Go back to lobby\n");
            printf("(9) Go to checkout\n");
            printf("Enter your choice: ");
            exitLobby();
            break;
    }
}

void cleaning() {
    printf("(1) Product 1..........$2\n");
    printf("(2) Product 2..........$3\n");
    printf("(3) Product 3..........$1\n");
    printf("(4) Product 4..........$4\n");
    printf("(5) Product 5..........$2\n");
    cleaningFunc();
}
void cleaningFunc() {
    int clean;
    scanf("%d", &clean);

    switch (clean) {
        case 1: 
            system("cls");
            for (int i = 0; i < 1; ++i) {
                sprintf(name[i], "tite", i + 1);
                quantity[i] = i + 1;
                price[i] = 2.5 * (i + 1);
            }
            printf("Item Added to Cart.\n");
            printf("\nReady to head out?\n");
            printf("(8) Go back to lobby\n");
            printf("(9) Go to checkout\n");
            printf("Enter your choice: ");
            exitLobby();
            break;
        case 2:
            system("cls");
            for (int i = 1; i < 2; ++i) {
                sprintf(name[i], "pepe", i + 2);
                quantity[i] = i + 2;
                price[i] = 2.5 * (i + 2);
            }
            printf("Item Added to Cart.\n");
            printf("\nReady to head out?\n");
            printf("(8) Go back to lobby\n");
            printf("(9) Go to checkout\n");
            printf("Enter your choice: ");
            exitLobby();
            break;
        case 3:
            system("cls");
            for (int i = 2; i < 3; ++i) {
                sprintf(name[i], "puke", i + 3);
                quantity[i] = i + 3;
                price[i] = 2.5 * (i + 3);
            }
            printf("Item Added to Cart.\n");
            printf("\nReady to head out?\n");
            printf("(8) Go back to lobby\n");
            printf("(9) Go to checkout\n");
            printf("Enter your choice: ");
            exitLobby();
            break;
        case 4:
            system("cls");
            for (int i = 3; i < 4; ++i) {
                sprintf(name[i], "lumpia", i + 4);
                quantity[i] = i + 4;
                price[i] = 2.5 * (i + 4);
            }
            printf("Item Added to Cart.\n");
            printf("\nReady to head out?\n");
            printf("(8) Go back to lobby\n");
            printf("(9) Go to checkout\n");
            printf("Enter your choice: ");
            exitLobby();
            break;
        case 5:
            system("cls");
            for (int i = 4; i < 5; ++i) {
                sprintf(name[i], "siomai", i + 5);
                quantity[i] = i + 5;
                price[i] = 2.5 * (i + 5);
            }
            printf("Item Added to Cart.\n");
            printf("\nReady to head out?\n");
            printf("(8) Go back to lobby\n");
            printf("(9) Go to checkout\n");
            printf("Enter your choice: ");
            exitLobby();
            break;
    }
}
void personal() {
    printf("(1) Product 1..........$2\n");
    printf("(2) Product 2..........$3\n");
    printf("(3) Product 3..........$1\n");
    printf("(4) Product 4..........$4\n");
    printf("(5) Product 5..........$2\n");
    personalFunc();
}
void personalFunc() {
    int personl;
    scanf("%d", &personl);

    switch (personl) {
        case 1: 
            system("cls");
            for (int i = 0; i < 1; ++i) {
                sprintf(name[i], "tite", i + 1);
                quantity[i] = i + 1;
                price[i] = 2.5 * (i + 1);
            }
            printf("Item Added to Cart.\n");
            printf("\nReady to head out?\n");
            printf("(8) Go back to lobby\n");
            printf("(9) Go to checkout\n");
            printf("Enter your choice: ");
            exitLobby();
            break;
        case 2:
            system("cls");
            for (int i = 1; i < 2; ++i) {
                sprintf(name[i], "pepe", i + 2);
                quantity[i] = i + 2;
                price[i] = 2.5 * (i + 2);
            }
            printf("Item Added to Cart.\n");
            printf("\nReady to head out?\n");
            printf("(8) Go back to lobby\n");
            printf("(9) Go to checkout\n");
            printf("Enter your choice: ");
            exitLobby();
            break;
        case 3:
            system("cls");
            for (int i = 2; i < 3; ++i) {
                sprintf(name[i], "puke", i + 3);
                quantity[i] = i + 3;
                price[i] = 2.5 * (i + 3);
            }
            printf("Item Added to Cart.\n");
            printf("\nReady to head out?\n");
            printf("(8) Go back to lobby\n");
            printf("(9) Go to checkout\n");
            printf("Enter your choice: ");
            exitLobby();
            break;
        case 4:
            system("cls");
            for (int i = 3; i < 4; ++i) {
                sprintf(name[i], "lumpia", i + 4);
                quantity[i] = i + 4;
                price[i] = 2.5 * (i + 4);
            }
            printf("Item Added to Cart.\n");
            printf("\nReady to head out?\n");
            printf("(8) Go back to lobby\n");
            printf("(9) Go to checkout\n");
            printf("Enter your choice: ");
            exitLobby();
            break;
        case 5:
            system("cls");
            for (int i = 4; i < 5; ++i) {
                sprintf(name[i], "siomai", i + 5);
                quantity[i] = i + 5;
                price[i] = 2.5 * (i + 5);
            }
            printf("Item Added to Cart.\n");
            printf("\nReady to head out?\n");
            printf("(8) Go back to lobby\n");
            printf("(9) Go to checkout\n");
            printf("Enter your choice: ");
            exitLobby();
            break;
    }
}
void breakfast() {
    printf("(1) Product 1..........$2\n");
    printf("(2) Product 2..........$3\n");
    printf("(3) Product 3..........$1\n");
    printf("(4) Product 4..........$4\n");
    printf("(5) Product 5..........$2\n");
    breakfastFunc();
}
void breakfastFunc() {
    
    scanf("%d", &brkfst);
    int brkfst;
    switch (brkfst) {
        case 1: 
            system("cls");
            for (int i = 0; i < 1; ++i) {
                sprintf(name[i], "tite", i + 1);
                quantity[i] = i + 1;
                price[i] = 2.5 * (i + 1);
            }
            printf("Item Added to Cart.\n");
            printf("\nReady to head out?\n");
            printf("(8) Go back to lobby\n");
            printf("(9) Go to checkout\n");
            printf("Enter your choice: ");
            exitLobby();
            break;
        case 2:
            system("cls");
            for (int i = 1; i < 2; ++i) {
                sprintf(name[i], "pepe", i + 2);
                quantity[i] = i + 2;
                price[i] = 2.5 * (i + 2);
            }
            printf("Item Added to Cart.\n");
            printf("\nReady to head out?\n");
            printf("(8) Go back to lobby\n");
            printf("(9) Go to checkout\n");
            printf("Enter your choice: ");
            exitLobby();
            break;
        case 3:
            system("cls");
            for (int i = 2; i < 3; ++i) {
                sprintf(name[i], "puke", i + 3);
                quantity[i] = i + 3;
                price[i] = 2.5 * (i + 3);
            }
            printf("Item Added to Cart.\n");
            printf("\nReady to head out?\n");
            printf("(8) Go back to lobby\n");
            printf("(9) Go to checkout\n");
            printf("Enter your choice: ");
            exitLobby();
            break;
        case 4:
            system("cls");
            for (int i = 3; i < 4; ++i) {
                sprintf(name[i], "lumpia", i + 4);
                quantity[i] = i + 4;
                price[i] = 2.5 * (i + 4);
            }
            printf("Item Added to Cart.\n");
            printf("\nReady to head out?\n");
            printf("(8) Go back to lobby\n");
            printf("(9) Go to checkout\n");
            printf("Enter your choice: ");
            exitLobby();
            break;
        case 5:
            system("cls");
            for (int i = 4; i < 5; ++i) {
                sprintf(name[i], "siomai", i + 5);
                quantity[i] = i + 5;
                price[i] = 2.5 * (i + 5);
            }
            printf("Item Added to Cart.\n");
            printf("\nReady to head out?\n");
            printf("(8) Go back to lobby\n");
            printf("(9) Go to checkout\n");
            printf("Enter your choice: ");
            exitLobby();
            break;
    }
}
void frozen() {
    printf("(1) Product 1..........$2\n");
    printf("(2) Product 2..........$3\n");
    printf("(3) Product 3..........$1\n");
    printf("(4) Product 4..........$4\n");
    printf("(5) Product 5..........$2\n");
    frozenFunc();
}
void frozenFunc() {
    
    scanf("%d", &froze);
    int froze;
    switch (froze) {
        case 1: 
            system("cls");
            for (int i = 0; i < 1; ++i) {
                sprintf(name[i], "tite", i + 1);
                quantity[i] = i + 1;
                price[i] = 2.5 * (i + 1);
            }
            printf("Item Added to Cart.\n");
            printf("\nReady to head out?\n");
            printf("(8) Go back to lobby\n");
            printf("(9) Go to checkout\n");
            printf("Enter your choice: ");
            exitLobby();
            break;
        case 2:
            system("cls");
            for (int i = 1; i < 2; ++i) {
                sprintf(name[i], "pepe", i + 2);
                quantity[i] = i + 2;
                price[i] = 2.5 * (i + 2);
            }
            printf("Item Added to Cart.\n");
            printf("\nReady to head out?\n");
            printf("(8) Go back to lobby\n");
            printf("(9) Go to checkout\n");
            printf("Enter your choice: ");
            exitLobby();
            break;
        case 3:
            system("cls");
            for (int i = 2; i < 3; ++i) {
                sprintf(name[i], "puke", i + 3);
                quantity[i] = i + 3;
                price[i] = 2.5 * (i + 3);
            }
            printf("Item Added to Cart.\n");
            printf("\nReady to head out?\n");
            printf("(8) Go back to lobby\n");
            printf("(9) Go to checkout\n");
            printf("Enter your choice: ");
            exitLobby();
            break;
        case 4:
            system("cls");
            for (int i = 3; i < 4; ++i) {
                sprintf(name[i], "lumpia", i + 4);
                quantity[i] = i + 4;
                price[i] = 2.5 * (i + 4);
            }
            printf("Item Added to Cart.\n");
            printf("\nReady to head out?\n");
            printf("(8) Go back to lobby\n");
            printf("(9) Go to checkout\n");
            printf("Enter your choice: ");
            exitLobby();
            break;
        case 5:
            system("cls");
            for (int i = 4; i < 5; ++i) {
                sprintf(name[i], "siomai", i + 5);
                quantity[i] = i + 5;
                price[i] = 2.5 * (i + 5);
            }
            printf("Item Added to Cart.\n");
            printf("\nReady to head out?\n");
            printf("(8) Go back to lobby\n");
            printf("(9) Go to checkout\n");
            printf("Enter your choice: ");
            exitLobby();
            break;
    }
}




void checkoutFunc() {
    system("cls");
    printf("You are in Checkout Area.\n");
    printf("Printing receipt....\nPlease wait...\n");
    checkoutCart();
    printf("\nThank you for shopping with us!\n");
    getchar();
}

void exitLobby() {
    int exitLob;
    scanf("%d", &exitLob);

    switch (exitLob) {
        case 8: // back to main displayfunc
            system("cls");
            displayMain();
            displayFunc();
        case 9: // checkout area
            checkoutFunc();
        case 0: // stays in area
            beverageFunc();
    } 
}

int main() {
    printf("Welcome to Shop.");
    displayMain();
    displayFunc();
    return 0;
}
